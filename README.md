# list-items-css-expanding-demo
A demo for an admin list with a quick view, implemented by expanding a list item

To set up the demo, use bower to install dependencies (Bootstrap and jQuery):

`bower install`
